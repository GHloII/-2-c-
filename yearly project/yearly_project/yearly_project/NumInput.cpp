#include <iostream>

double double_Num_input() {
	double input = 0;
	bool isError = false;

	do {
		if (isError) {
			std::cout << "Invalid input. Please enter a valid number." << std::endl;
		}

		if (!(std::cin >> input)) {
			isError = true;
			std::cin.clear(); // ??????? ???? ?????? ?????
			std::cin.ignore(std::numeric_limits<std::streamsize>::max(), '\n'); // ??????? ????? ????? ?? ??????? ????? ??????
		}
		else {
			isError = false;
		}

	} while (isError);

	return input;
}


int int_Num_input() {
	int input = 0;
	bool isError = false;

	do {
		if (isError) {
			std::cout << "Invalid input. Please enter a valid number." << std::endl;
		}

		if (!(std::cin >> input)) {
			isError = true;
			std::cin.clear(); // ??????? ???? ?????? ?????
			std::cin.ignore(std::numeric_limits<std::streamsize>::max(), '\n'); // ??????? ????? ????? ?? ??????? ????? ??????
		}
		else {
			isError = false;
		}

	} while (isError);

	return input;
}

unsigned long long Un_Num_input() {
	int input = 0;
	bool isError = false;

	do {
		if (isError) {
			std::cout << "Invalid input. Please enter a valid number." << std::endl;
		}

		if (!(std::cin >> input)) {
			isError = true;
			std::cin.clear(); // ??????? ???? ?????? ?????
			std::cin.ignore(std::numeric_limits<std::streamsize>::max(), '\n'); // ??????? ????? ????? ?? ??????? ????? ??????
		}
		else {
			isError = false;
		}

	} while (isError);

	return input;
}