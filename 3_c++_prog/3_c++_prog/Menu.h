#pragma once



enum MenuFunction { display_text = 1, display_dialogs = 2,
};

enum Menusf { file_open_empty =2, file_open = 1, 
 };

enum MenuCommand { start = 1, test = 2, Exit = 0 };

enum MenuFilling { hand_filling, file_filling };